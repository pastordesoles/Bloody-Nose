import { Typography, styled } from "@mui/material";

const SessionCardStyled = styled(Typography)`
  font-weight: bold;
  font-size: 2rem;
  .link {
    text-decoration: none;
    color: inherit;
  }
`;

export default SessionCardStyled;
